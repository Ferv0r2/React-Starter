import React, { FC } from "react";

const Header: FC = () => {
  return (
    <header className="sticky inset-0 bg-dark w-screen h-20 p-8 z-20">
      header
    </header>
  );
};

export { Header };
